# 酒馆工具箱开发规则

## 版本管理

- 每个包含文件改动的逻辑任务，在验证和提交前自动递增一次补丁版本号
- 补丁版本按语义化版本规则递增，例如 `0.1.2` 变为 `0.1.3`
- 版本号必须同步更新 `manifest.json` 和 `package.json`
- README 只用于面向用户介绍插件，不记录开发状态、版本同步规则、测试命令或项目结构
- 同一逻辑任务无论修改多少次文件，只递增一次版本号
- 仅执行只读检查、答疑、验证、提交或推送且没有文件改动时，不递增版本号
- 版本递增不依赖 Git push，也不要求自动推送远程仓库
- 提交前运行 `npm.cmd run check`，确保清单与包版本一致

## 提交

- 版本递增与本次功能改动放在同一个原子提交中
- 提交信息描述实际功能改动，不单独创建版本号提交

## 排版与模块

- JSDoc 使用标准多行块，不把说明和标签压缩在同一行
- 对象参数直接在对应函数上逐行展开 `options.field`，不使用行内匿名对象类型
- 类型说明紧邻实际函数或方法，本项目 JavaScript 文件不使用孤立 `@typedef`
- HTML 模板中的嵌套元素一行一个节点，多个相邻子节点不得挤在同一行
- 单个 JavaScript 模块聚焦一种职责；入口文件只负责依赖装配和生命周期
- 拆分模块时优先提取稳定边界，避免仅为缩短行数创建薄包装

## 目录结构

- `modules/chat` 放置聊天打开、分组、分卷及任务恢复逻辑
- `modules/backups` 放置备份识别与酒馆数据清理增强逻辑
- `modules/platform` 集中酒馆接口和扩展更新适配
- `modules/ui` 放置面板、弹窗及渲染逻辑
- `modules/shared` 仅放置无业务归属的通用工具
